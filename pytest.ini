[pytest]
testpaths = tests
python_files = test_*.py
addopts = --strict-markers --disable-socket --allow-unix-socket
markers =
    contract: deterministic provider, workflow, and report-boundary contracts
    redis_integration: tests that require an isolated real Redis instance
