[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

# ── Web Service (FastAPI only; v2.2 work runs in the ARQ service) ─────────────
[deploy]
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
