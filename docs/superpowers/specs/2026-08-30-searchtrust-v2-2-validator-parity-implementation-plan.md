# V22-030 校验一致性实施计划

日期：2026-08-30。依据：同日 validator-parity-design，用户已批准并要求实施。

状态：本轮实施和本地验证完成，结果见同日 validator-parity-completion 文档。

配套 writing-plans 技能在当前环境不可用，本清单按已批准设计手工编写；不改变范围，也不增加审批轮次。

## 1. 共享资源与失败测试

- 后端新增 `scripts/v22_validation_cases.py`：严格加载样例，支持 set/remove/reorder_keys，拒绝非法操作、路径和重复 ID。
- 新增 `tests/fixtures/report_v22_validation/cases.json`，覆盖 12 个已复现差异及既有语义规则和合法边界。
- 新增 `scripts/sync_v22_validation_resources.py`：生成 Unicode casefold/空白数据与哈希 manifest，只同步明确目标文件，支持只读检查。
- 新增后端参数化合同、加载器和资源检查测试；前端新增同样的加载器和参数化测试并纳入 `test:contract`。
- 先验证后端接受/拒绝预期，再记录前端修复前失败结果。

## 2. 前端局部修复

- 新增规范化辅助函数：精确字符 casefold、后端字符串去空白、目标地区字段比较、完整网址规范化比较。
- 补查询非空/唯一、Evidence 与三类第一方日期、三条页面计数关系、路线图顺序。
- 修复对象键顺序和可选 null/缺省比较，以及竞品网址等价形式去重。
- 保持错误类型、输入不变性、AJV 选项和所有已有语义约束；新增错误不回显输入值。

## 3. 验证与交付

- 后端：新样例/加载器/同步器测试、既有合同/API 测试、完整 pytest 回归。
- 前端：共享样例、加载器、规范化测试、既有合同测试、完整 Vitest 回归、非增量类型检查。
- 检查双仓库合同导出、辅助资源同步、TypeScript 重新生成无差异、冻结合同无改动。
- 检查实际改动和生产开关，记录完成说明及未覆盖边界；不推送、不部署、不调用外部数据提供方。

## 执行约束

后端 Pydantic/Schema/合同版本不变；不改 API、报告 UI、数据库、采集及执行器。不整体替换代码目录。测试辅助表与测试样例分离，前端运行时只引用生成表，不引用测试加载器。
